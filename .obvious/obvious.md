# prpatel/obv-workshop

## Repository status: empty

This repository is currently **empty**. The only tracked file is a `.gitignore`
(GitHub's Java-language template). There is no source code, no dependency
manifest, no build or container configuration, and no documentation.

The `.gitignore` template is the sole file and is not evidence of a stack —
no framework, runtime, or package manager should be inferred from it.

## Scope of this file

Because the repository contains no application:

- no stack is claimed (there is nothing to detect),
- no commands are listed (there is nothing to run),
- no ports are listed (there is nothing to serve),
- no local verification was performed (there is no dev stack to verify).

## Next setup run

The stack summary, commands, codebase map, and local verification notes will
be filled in by the next onboarding/setup run, once the repository contains an
application. When code lands, re-run onboarding so it can:

1. detect the runtime, package manager, and services from real manifests,
2. bring up and verify the local dev stack end-to-end,
3. capture a sandbox snapshot, and
4. regenerate this file with accurate, verified guidance.

`codebase-map.md` and `skills/local-dev/SKILL.md` are deliberately omitted —
there is no code to map and no local dev to document.
